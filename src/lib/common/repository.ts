/**
 * Where Studio's source and its issues live.
 *
 * One constant, because three places now send someone here — the alpha ribbon, the problems panel
 * and the Help menu — and the capability in `capabilities/default.json` scopes the opener to this
 * host and no other. A copy that drifted would not open at all.
 */
export const REPOSITORY = 'https://github.com/versatiles-org/versatiles-studio';
