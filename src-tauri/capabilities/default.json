{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "Baseline capability for project windows. Minimal by design \u2014 permissions are added per feature, not up front. `dialog:allow-open` is what A1's file picker needs.",
	"windows": ["main"],
	"permissions": ["core:default", "dialog:allow-open"]
}
