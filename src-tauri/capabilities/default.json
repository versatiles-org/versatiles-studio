{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "default",
	"description": "Baseline capability for the main window. Kept minimal — permissions are added per feature, not up front.",
	"windows": ["main"],
	"permissions": ["core:default"]
}
