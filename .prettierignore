CHANGELOG.md
node_modules/
package-lock.json
dist/
src-tauri/gen/
src-tauri/target/
src-tauri/icons/
public/maplibre-gl-worker.js
target/
# Coverage reports - generated, and the HTML one carries third-party JS.
coverage/
lcov.info
# Generated - its formatting is the generator's, and reformatting it makes the staleness test fail.
src/lib/ipc/bindings.ts
# Test data - fixtures, not source. Reformatting one changes the bytes a test reads, and only the
# `.geojson` is a format prettier even recognises, which would leave one file in the folder written
# by something other than GDAL. The README beside them is prose and stays formatted.
testdata/*
!testdata/README.md
